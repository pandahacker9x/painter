import 'dart:typed_data';

import 'package:flutter/material.dart';

void tmp() {
  var paint = Paint();
  paint.color;
  paint.strokeWidth;
  var path = Path();
  var floatList = Float64List(100);
  path.transform(floatList);
}
